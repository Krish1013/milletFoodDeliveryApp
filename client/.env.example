# Local development — points to local backend
VITE_API_BASE_URL=http://localhost:5000

# Production (set in Vercel dashboard) — points to Render backend
# VITE_API_BASE_URL=https://krishna-millet-app.onrender.com
